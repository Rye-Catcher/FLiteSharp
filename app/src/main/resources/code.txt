1 + 1;
1 - 1;
1 * 2;
1 / 2;
2 ** 10;
1 - -1;
1 >= 2;
1 < 2;
true && false;
true || false;

let x = 1;
let y = 2;

fun x y -> x + y;
fun () -> 1;

let foo x = {
    x + 1;
};
let bar ()  = {
    let x = -19260817;
    return x;
    123456;
};

foo(1);
foo(x + 1);

let lam f = {
    f(1);
};
lam(fun x -> x + 1);

bar();

let nest x = {
    let foo x = {
        let bar x = {
            return x + 1;
        };
        return bar(x + 1);
    };
    return foo(x + 1);
};

nest(x);